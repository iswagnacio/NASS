# NASS dependencies — pinned to local Mac versions
# Install with: pip install -r requirements_nass.txt
#
# NOTE: Install JAX with GPU support FIRST, separately:
#   pip install "jax[cuda12]==0.4.35"
# Then install this file. The jax/jaxlib lines below are
# commented out to avoid overwriting the CUDA wheel.

# --- JAX ecosystem (install jax[cuda12] separately) ---
# jax==0.4.35
# jaxlib==0.4.35
jaxley==0.13.0
optax==0.2.7
tridiax==0.2.1
ml_dtypes==0.5.4

# --- Core scientific ---
numpy==1.26.4
scipy==1.17.1
pandas==2.3.3
matplotlib==3.10.8
h5py==3.16.0

# --- Allen SDK ---
allensdk==2.16.2
pynwb==3.1.3
hdmf==4.3.1

# --- LLM outer loop ---
anthropic==0.85.0
python-dotenv==1.2.2

# --- Utilities ---
tqdm==4.67.3
PyYAML==6.0.3
requests==2.32.5
